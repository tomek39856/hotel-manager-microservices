server:
  port: 1793
spring:
  profiles:
    active: local
  application:
    name: occupancy
  activemq:
    broker-url: tcp://localhost:61616
    user: admin
    password: admin
  jms:
    pub-sub-domain: true
  mvc:
    static-path-pattern: /resources/**