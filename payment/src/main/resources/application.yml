server:
  port: 1490
spring:
  application:
    name: payment
  activemq:
      broker-url: tcp://localhost:61616
      user: admin
      password: admin
  jms:
    pub-sub-domain: true
  mvc:
    static-path-pattern: /resources/**